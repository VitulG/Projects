package com.social.connectify.models;

public enum GroupNotificationType {
    PROMOTION,
    JOIN_REQUEST_APPROVED,
    JOIN_REQUEST_APPROVAL,
    USER_LEFT_GROUP,
    ADMIN_REMOVED_USER,
    GROUP_MEMBER_ADD_USERS,
    GROUP_DELETED
}
